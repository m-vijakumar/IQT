let all={ op9: 'Interfaceisanconcreteclass.' }

// var allans =Object.keys(all).map((key)=>{return [all[key]]})
            

// console.log(allans[0]);


let nn = '9op'
var integer = parseInt(nn, 10);

console.log(integer)